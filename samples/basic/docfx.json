{
  "metadata": [],
  "build": {
    "content": [
      {
        "files": [ "**/*.{md,yml}" ],
        "exclude": [ "_site/**", "obj/**", "bin/**" ]
      }
    ],
    "output": "_site",
    "template": [ "default", "modern" ],
    "globalMetadata": {
      "_appTitle": "Remote Include — Basic Sample",
      "_appName": "Remote Include",
      "_enableSearch": true
    }
  }
}
