Clone the repository and restore dependencies:

```bash
git clone https://github.com/saipramod/docfx-remote-include.git
cd docfx-remote-include
dotnet restore
```

Then build the solution:

```bash
dotnet build
```
